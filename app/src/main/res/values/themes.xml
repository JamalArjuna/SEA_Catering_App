<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema dasar aplikasi Anda. Pastikan ini adalah turunan dari Material Components atau AppCompat. -->
    <style name="Base.Theme.SeaCatering" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
    </style>

    <!-- Tema aplikasi utama . -->
    <style name="Theme.SeaCatering" parent="Base.Theme.SeaCatering" />

    <!-- TEMA SPLASH SCREEN -->
    <style name="Theme.App.Starting" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowSplashScreenBackground" tools:targetApi="31">@color/Pigmen_Green</item>
        <item name="android:windowSplashScreenAnimatedIcon" tools:targetApi="31">@drawable/img</item>
        <item name="postSplashScreenTheme">@style/Theme.SeaCatering</item>
    </style>

</resources>